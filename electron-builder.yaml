directories:
  buildResources: buildResources
  output: dist
# For production build, use this:
# extraFiles: ['LICENSE', 'LICENSE.ScratchFoundation', 'TRADEMARK', "tools", "external-resources", 'firmwares', "drivers"]

# For development build (faster):
extraFiles: ['LICENSE', 'LICENSE.ScratchFoundation', 'TRADEMARK', "external-resources", "drivers"]

appId: betatech.autoblox-desktop
productName: "AutobloxDesktop"
publish:
  - provider: github
artifactName: "Autoblox-Desktop_v${version}_${os}_${arch}.${ext}"

fileAssociations:
  ext: ab
  name: Autoblox project file
  role: Editor
  icon: buildResources/AutobloxFile.ico

compression: maximum

mac:
  category: public.app-category.education
  entitlements: buildResources/entitlements.mac.plist
  extendInfo:
    NSCameraUsageDescription: >-
        This app requires camera access when using the video sensing blocks.
    NSMicrophoneUsageDescription: >-
        This app requires microphone access when recording sounds or detecting loudness.
  gatekeeperAssess: true
  hardenedRuntime: true
  icon: buildResources/AutobloxDesktop.icns
  provisioningProfile: embedded.provisionprofile
  target:
    - dmg
    # - mas
dmg:
  title: "Autoblox-Desktop_${version}"
  format: UDRO
# mas:
#   category: public.app-category.education
#   entitlements: buildResources/entitlements.mas.plist
#   entitlementsInherit: buildResources/entitlements.mas.inherit.plist
#   hardenedRuntime: false
#   icon: buildResources/OpenBlockDesktop.icns
# masDev:
#   type: development
#   provisioningProfile: mas-dev.provisionprofile

win:
  icon: buildResources/AutobloxDesktop.ico
  target:
    # - appx
    - nsis
# appx:
#   identityName: "OpenblockTeam.OpenblockDesktop
#   publisherDisplayName: "OpenBlock Team"
#   publisher: "CN=2EC43DF1-469A-4119-9AB9-568A0A1FF65F"
nsis:
  oneClick: false # allow user to choose per-user or per-machine
  allowToChangeInstallationDirectory: true
  include: buildResources/installer.nsh
  buildUniversalInstaller: false
#   license: LICENSE

linux:
  category: Education
  icon: buildResources/linux
  desktop:
    entry:
      Encoding: UTF-8
      Name: AutobloxDesktop
      Icon: autoblox-desktop
      Type: Application
      Terminal: false
  target:
    - deb
deb:
  depends: ["libnotify4", "libxtst6", "libnss3"]
